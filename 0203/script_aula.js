var idade = 29;
console.log(idade);

var soma = 100 + 50;
var subtracao = 100 - 50;
var multiplicacao = 100 * 2;
var divisao = 100 / 2;
var expoente = 2 ** 4;
var modulo = 14 % 5;

var soma = "100" + 50; // 10050
var subtracao = "100" - 50; // 50
var multiplicacao = "100" * "2"; // 200
var divisao = "Comprei 10" / 2; // NaN (Not a Number)

// NaN = Not a Number

// É possível verificar se uma variável é NaN ou não com a função isNaN()

// Ordem de Precedência

var x = 5;
console.log(++x);
console.log(x);

// Unários
// --  e ++  antes da variável = incrementa ou decrementa a variável e retorna o valor

// se botar - ou + na frente da string ele tenta transformar em número
